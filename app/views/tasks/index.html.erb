<div class="bg-primary-subtle p-4 mx-auto p-2" style="width: 50%;">

<h1 class="d-flex justify-content-between align-items-center mb-4">My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Add New Task", new_task_path() %>

</div>
